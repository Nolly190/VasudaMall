@model VasudaDataAccess.DTOs.NotificationViewModel


@{
    Layout = "~/Views/shared/_LayoutDashboard.cshtml";
    ViewBag.Title = "Notification";
}

<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">Notifications</h3>
                    </div>
                    <!-- /.card-header -->
                    <div class="card-body">
                        @if (Model.AllNotifications.Count == 0)
                        {
                            <h3 style="text-align:center">NO NOTIFICATION AVAILABLE</h3>
                        }
                        else
                        { 
                            <table id="notificationTable" class="table table-bordered table-striped table-hover">
                                <thead>
                                    <tr>
                                        <th>S/N</th>
                                        <th>Subject</th>
                                        <th>Date Published</th>
                                        <th>Read Status</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var notification in Model.AllNotifications)
                                    {
                                        var count = 1;
                                        <tr>
                                            <td>@count</td>
                                            <td>@notification.Subject</td>
                                            <td>@notification.DateCreated</td>
                                            <td>@notification.IsRead</td>
                                            <td><button type="submit" class="btn btn-outline-success btn-sm">View More</button></td>
                                        </tr>
                                        count++;
                                    }
                                </tbody>
                                <tfoot>
                                    <tr>
                                        <th>S/N</th>
                                        <th>Subject</th>
                                        <th>Date Published</th>
                                        <th>Read Status</th>
                                    </tr>
                                </tfoot>
                            </table>                       
                        }
                        </div>
                    <!-- /.card-body -->
                </div>
                <!-- /.card -->
                </div>
            </div>
        </div>
 </section>
